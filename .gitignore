# HistoryJanus baseline begin
*.bak
*.buildwithskipanalyzers
*.cache
*.dwl
*.dwl2
*.elk
*.eod
*.eox
*.flk
*.idb
*.ilk
*.lck
*.log
*.lref
*.obj
*.pdb
*.slk
*.suo
*.tlog
*.tmp
*.up2date
*.user
*.vsidx
*.xlk
# HistoryJanus baseline end

# IDE state
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates

# .NET build and test outputs
**/bin/
**/obj/
TestResults/
/artifacts/

# Local package and deployment scratch
*.tmp
*.log

# Deployment databases and rollback archives are machine-local recovery assets.
b-Office/evidence/**/*.db
b-Office/evidence/**/*.zip

# z-Publish tracks the current candidate and immutable history; build staging lives in the system temp directory.

# Local Inno Setup bootstrap download (not a release artifact)
b-Code-Eng/tools/

# diana.worktree.create 在 AI 工作区生成的本机覆盖点：内容是这台机器上的绝对路径，
# 入库会让别人的构建指向不存在的目录。另外四个项目早已登记，本仓一直漏登记。
Directory.Build.user.props

# HistoryJanus managed begin
# 本块由 HistoryJanus 按设置 proj.excludesuffixes 自动生成，全库一致。
# 不要手工编辑块内内容；改清单请用 janus.gitrule.excludes。
# 块外内容属于本项目自己，Janus 逐字保留。
bin/
obj/
venv/
.venv/
__pycache__/
.vs/
.idea/
node_modules/
.pytest_cache/
.mypy_cache/
**/tools/jdk/
packages/
*.dll
*.pdb
*.lib
*.exp
*.ilk
*.idb
*.obj
*.cache
*.user
*.suo
*.tmp
*.temp
*.log
*.bak
*.swp
*.xlk
*.autosave
Thumbs.db
.DS_Store

# 正式消费快照由发布管线刻意入库，不受上面任何排除项影响。
!z-*/
!z-*/**
# HistoryJanus managed end
