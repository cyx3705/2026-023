# HistoryVulcan 5.1.2 宿主冻结执行证据

日期：2026-08-31

## 冻结结论

- 冻结基线：`5.1.2`，标签名 `v5.1.2`。
- 冻结范围：模块注册器、命令总线、开发/发布总线，以及 Core / Services / ServiceHost 公开 API。
- 不纳入宿主：模块拥有的 UI、MCP/传输与业务协议；冻结不绑定模块数量、名称或版本清单。
- 冻结前最后收口：退役 `vulcan.command.manual`；命令手册只走
  `HistoryVulcan.exe --export-command-manual <Markdown 路径>`。

## 公开 API 结转

| 程序集 | Shipped 有效条目 | Unshipped 有效条目 | 5.1.2 批准基线有效条目 |
| --- | ---: | ---: | ---: |
| HistoryVulcan.Core | 237 | 0 | 0 |
| HistoryVulcan.Services | 239 | 0 | 0 |
| HistoryVulcan.ServiceHost | 122 | 0 | 0 |

`project.manifest.json` 与 `b-Code-Eng/pipeline/module-publish.manifest.json` 的 `freezeTag`
均为 `v5.1.2`。

## 本地验证

| 验证 | 结果 |
| --- | --- |
| `dotnet restore .\HistoryVulcan.sln --locked-mode` | 通过 |
| Debug build | 通过，0 warning / 0 error |
| Debug test | 147/147 通过，0 跳过 |
| Release build | 通过，0 warning / 0 error |
| Release test | 147/147 通过，0 跳过 |
| `dotnet format --verify-no-changes --no-restore` | 通过 |
| `ReleasePipelineTests` | 10/10 通过 |
| `QualityGatesPassOnThisRepository` | 1/1 通过 |
| `InteractiveManualGenerationIsNotPartOfTheFrozenHostCatalog` | 1/1 通过 |
| `git diff --check` | 通过 |
| 首次正式部署 | run `20260831-173437-HistoryVulcan`：门禁与 147/147 测试通过，提升时因正式宿主文件锁失败 |
| 正式部署重试 | run `20260831-173924-HistoryVulcan`：部署成功，提交 `3c92b78` |
| 新宿主运行时 | PID 31836，版本 5.1.2；7 个模块全部 attached，命令数均非零 |

## 未执行

- Git commit：正式管线已提交 `3c92b78`（`freeze-host-5.1.2`）。
- Git tag `v5.1.2`：必须指向批准后的冻结提交，本轮未授权。
- 正式宿主提升：首次被旧宿主 PID 12636 的文件锁阻止；人工停服后重试成功。
- 新宿主：从 `z-Publish/host/HistoryVulcan.exe` 启动，PID 31836；运行时管道查询成功。
- 推送：未授权。
- 固定模块名单或跨仓消费方验证：不属于宿主冻结门槛；各模块仓库自行负责业务 Smoke。

首次失败留下的旧候选归档与重试时生成的归档均由正式管线随 `3c92b78` 提交；当前
`z-Publish/host` 已替换为冻结后的 5.1.2 宿主。
