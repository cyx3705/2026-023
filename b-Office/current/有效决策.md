# HistoryVulcan 有效决策

保留决策依据与编号，行为细节统一在[技术合同](技术合同.md)。旧全文见[5.2.1 合同归档](../history/5.2.1-现行合同归档.md)，历史编号不重新分配。

## DEC-063 5.4.0 总线前端单一所有权与宿主内部面收回

2026-09-14：用户明确指定 5.4 并授权跨模块一次改到位。这是常规需走主版本的兼容变化的例外，不作为后续删除先例。

依据是对运行区 8 个已部署模块做的元数据与 IL 调用点核对：宿主总线的 FrontendExecutor、Confirmation、ConfirmationRouter、UiContext、RemoteExecutor、ShouldUseRemoteCommand 只有 Aurora 在写，会覆盖宿主缺省的拒绝闸口；Janus 直接读 Bus.Confirmation，确认结果取决于 Aurora 是否已改写；ServiceHost 程序集没有任何模块引用；Services 公开面除模块 Smoke 使用的装载器外，只有 Aurora 与 Janus 零星引用。

- 宿主总线交给模块前封口，上述开关只读。前端经 `IModuleContext.RegisterFrontend(IFrontend)` 唯一登记，模块卸载、热装失败或 Attach 失败时撤销；处理器执行中途确认改用 `CommandBus.RequestConfirmation`。
- ServiceHost 全部类型，Services 的 AppPaths、SettingsService、ShellLog、开发管线入口、OfflineModuleInstall、命令目录注册入口，以及 Core 的 CommandHistory、CommandSelectionState、IDeferredStartupWork、CliExposurePolicy 收回 internal 或删除。ModuleHost 只保留模块 Smoke 需要的装载器面。
- 同轮在模块工作区迁移 Aurora、Janus、Minerva 的消费代码。

精确变更保存在 5.4.0 PreFreeze 基线，消费摘要见[模块 API](../package/模块API.md)。此次授权不含提交、标签、推送、正式发布和重启宿主部署。

## DEC-062 冻结后的宿主内部重构

2026-09-13：用户授权清除无用实现、精简整个宿主的代码、测试和文档。在 DEC-061 工作区基础上实施，源码与冻结线保持 5.3.0，公开 API 及批准基线不变；不构成删除 API 或恢复网关的例外。实施与验证见[重构说明](宿主重构说明.md)。此次授权不含提交、标签、推送、正式发布和部署。

## DEC-061 5.3.0 重新冻结宿主

2026-09-13：用户要求将 5.3.0 作为 5.2 之后的现行冻结线。两份 manifest 登记 v5.3.0，替代 DEC-056 及 DEC-060 的暂留基线；历史 v5.1.2 标签不移动。批准 API 结转 Shipped，结转前文本留存 PreFreeze。基线计数、后续兼容纪律及交付边界见[冻结合同](冻结合同.md)。

## DEC-060 5.3.0 修复与迁出遗留清理

2026-09-06：用户明确指定 5.3.0 并授权删除过期公开面，属于通常需要主版本的兼容变化例外，不作为后续删除先例。精确变更保存在 5.3.0 PreFreeze 基线，消费摘要见[模块 API](../package/模块API.md)。当时暂留 v5.1.2，后由 DEC-061 更新现行基线。

删除前核对宿主源码、测试、已部署模块元数据引用和正式消费合同。Aurora 仍消费 FrontendExecutor、RemoteExecutor、ShouldUseRemoteCommand，以及模块管理、目录 DTO、设置和路径合同，故保留；未观察到已删除接口的部署引用，结论仅限已核对版本。WPF 解析与可回收 ALC 属于模块装载能力，继续保留。

本次同时修复事务数据恢复、Attach 失败、同包重试及脱敏；配置保持原存储和既有键，runtime 保留原握手及批准。验收见技术合同 REQ-TXN-001、REQ-MOD-010、REQ-CMD-014、REQ-CFG-001、REQ-CLI-001。

## 仍有效的早期决策

| 决策 | 约束与现行定义 |
| --- | --- |
| DEC-056 | 5.1.2 为历史冻结标签；当前基线由 DEC-061 替代，变更纪律见冻结合同 |
| DEC-057 / DEC-059 | 按依赖次序接入、逐个发布、整轮就绪与统一快照清除；REQ-MOD-008/009/010 |
| DEC-058 | 请求只解析一次，敏感规则取自参数及键语义；REQ-CMD-001/014。5.2 批准删除记录保存在 PreFreeze |
| DEC-052 / DEC-053 / DEC-054 / DEC-055 | 开发恢复能力留在宿主，进程内发布引擎与受限 runtime 分离；REQ-CLI-001、REQ-REL-001；流程只在模块开发手册 |
| DEC-040 / DEC-043 / DEC-039 | 独立仓库、固定运行区完整包发现、候选与运行包分离；REQ-PKG-001、REQ-MOD-006/007；热更新不停止宿主 |
| DEC-049 / DEC-050 / DEC-051 | 界面归 Aurora、工作台/快捷键归 Mercury、Web/MCP 归 Portunus；提示治理归模块。命令域跟随所有者，既有界面生命周期命令由 FrontendExecutor 转交 |
| DEC-025 / DEC-023 | 三段业务命令与两段直接方法并存，域由注册表/owner 决定；REQ-CMD-010/011；门禁只验证本仓 |
| DEC-005 / DEC-007 / DEC-008 | 编辑真值见文档中心；固定 SDK、锁定依赖、CI 只验证不发布或写 Git，执行方法见验证合同 |

旧重复编号不复用：REQ-CMD-012 的域聚焦合入 REQ-CMD-010，界面规则归 Aurora；原重复 REQ-MOD-008 的装载次序改为 REQ-MOD-009。
