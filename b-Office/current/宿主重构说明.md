# 5.3.0 宿主重构记录

日期：2026-09-13，依据 DEC-062。以任务开始时已有的 DEC-061 工作区为比较基准，保留用户已有冻结改动；源码及冻结线保持 **5.3.0 / v5.3.0**。

## 改动

| 区域 | 实施 |
| --- | --- |
| 命令总线 | 拆出 CommandRequest / CommandArguments / SensitiveName，固定当次描述符和敏感值；修复执行中注销、稀疏 Position 漏遮，配置入口复用敏感规则 |
| 模块生命周期 | 按发现、元数据、快照、描述符与加载上下文组织；构造元数据一次，统一注销/释放；单模块依赖也诊断，卸载清除 XAML 解析引用 |
| 开发/发布 | ToolProcess 统一 Git 与发布的双流/超时；删除同步结果后的轮询、反射/正则取 ID、重复 DTO 与恒定分支；运行 ID 独立，日志尾部内存有界 |
| 离线 CLI | 校验后组合，禁用文件监听；DevelopmentContext 归各自 ServiceComposition；失败 JSON 与进程退出码一致 |
| 构建/测试 | 退役 NuGet pack 配置；合并 24 份设置/日志夹具，保留专用故障夹具，增加 13 个行为回归 |
| 文档 | 合并重复规则、流程与验证记录；修正 submit 必填工作区、finish 复用候选及 CLI 退出码说明；manifest 链接门禁覆盖全部活动 Markdown |

文件定位见[源码导航](../../b-Code-HistoryVulcan/README.md)，规则分工见[文档中心](../文档中心.md)。

## 规模（重构与文档精简完成时）

UTF-8、统一 LF，排除 bin/obj；以下为源码体积，未测量 EXE、安装包或运行内存。

| 范围 | 任务开始 | 重构后 | 净变化 |
| --- | ---: | ---: | ---: |
| 产品 .cs 行数 | 12,192 | 11,653 | -539 |
| 产品 .cs 字节 | 521,020 | 494,453 | -26,567（5.1%） |
| 测试 .cs 行数 | 5,169 | 5,150 | -19 |
| 活动 Markdown 字节 | 70,005 | 58,422 | -11,583（16.5%） |

按职责拆分使产品 .cs 文件由 72 增至 79；文件数不作为体积指标。
文档统计含 current/package、活动代码目录的 Markdown、根 README/AGENTS 和文档中心；不含历史及发布副本。增加本记录后共 18 份。相比文档精简前的 79,843 字节，减少 21,421 字节（26.8%）。

## 验证证据

- DEC-061 冻结后、重构前：Debug/Release 构建均零警告零错误，全量各 201/201；ReleasePipelineTests 11/11、格式和 git diff --check 通过。重构起步命令/管线定向 126/126。
- DEC-062 源码完成：Debug/Release 构建均零警告零错误，全量各 214/214、无跳过；格式、API/质量/项目合同门禁与 git diff --check 通过。42 份公开 API/版本基线文件与任务开始时逐字节一致。
- 文档精简：Release 定向门禁 15/15；20 个 REQ 的 57 条验收映射、22 个 DEC 和 12 个 VERIFY 编号均保留，18 份活动文档的 65 个本地链接有效。AGENTS 未改；本阶段只改文档和 manifest，未重复全量构建测试。
- 两轮标准锁定还原均因 NuGet 在线漏洞服务不可达报 NU1900；命令级 `-p:NuGetAudit=false` 下通过，默认审计配置未改，在线漏洞审计未完成。

上述重构与文档精简阶段未修改邻接仓库或已发布副本，未停止宿主，未提交、打标签、推送或部署；消费模块联调及真实 WPF 界面验证留待部署。交付检查定义见[验证合同](验证合同.md)。

## 后续部署：2026-09-13

用户随后明确授权重启并部署宿主。部署前 Release 全量 214/214，通过现有 HostSnapshotBuilder 生成 5.3.0 宿主、CLI、合同程序集及两份消费文档；PublishLayout 事务替换后，完整 SHA256SUMS 与候选一致。旧包归档到 `z-Publish/history/HistoryVulcan-v5.3.0-20260913-185314`。构建时源码尚未提交，快照如实标记 sourceDirty=true。

新进程的 runtime 握手返回 5.3.0，ready=true，8/8 模块已接入，无附着失败；各模块命令数与重启前一致。期间运行区包另有更新，重启后 Aurora 1.21.0、Diana 2.6.1 均与磁盘包一致；本次部署未修改模块包。

Diana windows/capture 后实际读取主窗口 PNG：1279×836，PrintWindow，SHA256 `82D93BFA6EDBAFED9A55B5DD8B2E56E9A1F621FE0C7F9C0089641F37486B68DC`；布局和页面可见。Juno 无法连接 Sub2API 的警告在重启前已存在。此次只验证启动、模块接入、runtime/MCP 可达与界面显示，未执行确认点击、真实 WPF 卸载或完整模块业务联调。
