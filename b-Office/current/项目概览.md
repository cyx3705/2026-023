# HistoryVulcan 项目概览

更新日期：2026-08-31

## 一句话定义

HistoryVulcan 是 OneHistory 的通用 .NET 8 / WPF 壳：命令、停靠、日志、设置、模块、MCP/Web
与服务生命周期。它是**独立 git 仓库**里的一个编号项目，不是共享裸仓上的一条分支。

## 项目库编码（一项目一仓）

- 本目录 `2026-023-HistoryVulcan` 本身就是仓库；默认分支是 `main`。平行开发在本仓内开分支。
- 邻接的 `YYYY-NNN-*` 目录是别的项目、别的仓库。不得把它们当成同一 Git 身份下的 worktree 或兄弟分支。
- 模块发现只认固定运行区文件系统：只枚举 `%AppData%\HistoryVulcan\Modules` 的直属模块目录。
  发现不依赖任何裸仓、不读取兄弟仓的 git 对象。
- CI 与门禁只验证本仓库。兄弟项目在本地磁盘上存在，不表示它们属于本仓，也不得用跨仓 `ProjectReference`。

## 目标与边界

### 目标

- 提供可运行、可测试、可回滚的独立宿主，并为嵌入式消费保留固定公共合同。
- 单一窗口承载中央工作区与四边工具页；布局持久化；模块可热重载。
- 默认最小能力、安全边界、公开 API 基线守住冻结契约。

### 范围

- WinExe 宿主与同目录 Console CLI，以及本仓仍维护的 `Core`、`Services`、`ServiceHost` 合同；桌面 Shell 在 HistoryAurora。
- 根级 `b-Code-Tests`（`HistoryVulcan.Tests`）。
- 消费合同编辑源、候选打包、正式归档及 `z-Publish` 当前快照。

### 非目标

- 不承载 Janus、Mercury 或其他模块的业务与数据模型。
- 不提供已删除的数据库服务、通用表窗口或 `db.*`。
- CI 不发布、不打标签、不部署。
- 不把项目库 Git 治理做成宿主职责（那是 Janus，且存储引擎正在换到一项目一仓）。

## 当前版本

源码为 **5.2.0**，宿主冻结基线为 **5.1.2**，冻结标签名为 **`v5.1.2`**（DEC-056）。
5.2.0 是重新冻结前的最后一轮内核清理（DEC-058）：指令总线一行只解析一次，脱敏规则由名单式
改为判据式，删掉十个零消费方的公开成员，净减 179 行、Core/Commands 收到 11 个文件。
5.1.3 修模块接入次序与就绪屏障（DEC-057）：装载与接入分成两段，接入按 `dependsOn` 逐个进行，
接一个登记一个；全部接上后置就绪，`vulcan.module.ready` 可查、`<域>.host.ready` 收通知。
三份 Shipped 一个符号未动。Core 在 4.0
阶段性解冻完成前端切出（DEC-049），5.0 拆除模块领域抽象（DEC-052）后已重新冻结。
模块开发管线是 `--cli vulcan.dev.*`（禁止 MCP、禁止宿主走）；宿主写入 z 候选用
`--cli vulcan.release.cycle`（发布入口）。
同目录 `HistoryVulcan.Cli.exe` 是 Console CLI：`--cli` 固定执行离线组合，`--runtime` 经当前用户命名管道连接已运行宿主且不可达时不降级；动作须 `--approve`，自动化使用 `--format json`。
`v3.0.3` 只约束 3.0.x 历史维护分支，不是当前冻结标签。
版本线不与 HistoryJanus 对齐。3.1.8 不受支持。

里程碑与逐次验证证据见 [验证合同](验证合同.md)，架构决策见 [有效决策](有效决策.md)。

## 交付物

| 交付物 | 权威位置 | 完成判据 |
| --- | --- | --- |
| 框架源码 | `b-Code-HistoryVulcan/` | Debug/Release 0 warning、0 error，测试通过 |
| 宿主候选/运行入口 | `z-Publish/` | `host/`、文档、manifest、SHA-256 匹配 |
| 历史发布包 | `z-Publish/history/<发布标识>/` | 每个直属目录均为可独立校验的完整包 |
| 运行时模块 | `%AppData%\HistoryVulcan\Modules/<模块名>/` | 只由 Vulcan 安装/移除接口维护 |
| 消费合同源 | `b-Office/package/` | 发布流程复制后与正式副本哈希一致 |
| 现行项目合同 | 本文件、技术合同、有效决策、验证合同 | 与 manifest、源码、测试一致 |

现行 `current/` 只保留这四份加冻结合同与断头指令审计表。发布步骤和总线健康结论并入本文与技术/验证合同，不再单开专题。

## 发布（宿主打包入口，不是模块开发管线）

权威入口是宿主命令行，不再经 PowerShell 发布脚本。模块不要调这条，走手册三条 `vulcan.dev.*`：

```text
# 工作区候选（构建、门禁、写入该工作树 z-Publish）
HistoryVulcan.Cli.exe --cli vulcan.release.cycle name=HistoryVulcan msg=candidate worktree=<工作区>

# 正式提升（主树：先部署后提交；允许脏工作树；须用户批准）
HistoryVulcan.Cli.exe --cli vulcan.release.cycle name=HistoryVulcan msg=publish
```

**宿主发布不能从 `z-Publish/host` 起的宿主执行**：那是在替换自己正在运行的文件。
自替换按 DEC-053 走手动操作——先停服，再从该目录之外的宿主（例如 App 项目的
`bin/Release` 开发构建）执行。管线在第一步就会拒绝并说明，不会白跑一遍构建。

合同、质量门禁、公开 API 基线和宿主/模块打包都在 `HistoryVulcan.Services.Development.Pipeline`；
按 DEC-053，这套管线留在宿主内，不移出为模块。
消费文档只改 `b-Office/package/`；写入 `z-Publish/docs/` 由同一管线复制。
不得手改 `z-Publish/` 根候选或历史包。可选安装包仍可用 `b-Code-Eng/Pack-HistoryVulcanInstaller.ps1`。

嵌入框架的体系外消费方若仍用四包，必须同一版本；宿主自身以 Z 里的 EXE 为准，不发布到 NuGet.org。
回滚：把消费方锁回上一正式版本，或从 `z-Publish/history/<版本>/` 取当时快照；用户数据目录先备份。

## 命令总线运行事实

独立宿主里 MCP、非 UI 模块和命令权威表都在后台；前端 `EnableMcp=false`，经 loopback 转发。
`tools/list` / `tools/call` 每次读后台注册表，热重载不换端口。
已认证的本机 `loopback-shell` 不消耗公网 `web.ratelimit`；普通 Web / 远端会话仍限流，429 必须带 `Retry-After`。

仍开放的风险（不是未修缺陷，是已知边界）：

1. `CommandBus` 没有全局并发上限；削峰靠公网限流和 MCP 会话，不是完整背压。
2. `McpExposurePolicy` 只支持一个权威 `ModuleHost`，没有同进程多租户。
3. 前端 WebSocket 只在连接时发布 UI 命令目录；运行期新增前端命令需重连。后台模块命令不受此限。

专项测试：`VERIFY-MCP-BUS`（`McpCommandBusIntegrationTests`、`StandaloneMcpOwnershipTests`、`WebGatewayRateLimitTests`）。

## 关键约束

- 平台：Windows、WPF、`net8.0` / `net8.0-windows`。SDK 以根 `global.json` 为准。
- 独立可执行宿主 `EnableModules=true`；包消费方默认 `EnableMcp=false`、`EnableModules=false`。
- MCP 须描述符显式允许；敏感值不得进日志、历史或未授权响应。

## 外部依赖

| 依赖 | 版本或位置 | 用途 |
| --- | --- | --- |
| .NET SDK | `global.json` | 构建与测试 |
| AvalonDock | `Directory.Packages.props` | Shell 停靠 |
| HistoryDiana | 邻接项目仓 `2026-019-HistoryDiana` | 模块消费方，不再承载发布引擎 |
